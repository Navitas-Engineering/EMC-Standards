Program which sweeps through a folder for any pdfs of engineering standards, finds the associated standard code and renames the file to be ready to be inputted into an excel library (which relies upon file names matching the code number).

Codes are renamed into the format AA_BB_00000-0_YYYY where AA/BB are code letters, 00000-0 is the standard number (and part) and YYYY is the year of publication.
